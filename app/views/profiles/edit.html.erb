<main class="form-edit">
  <%= render "devise/shared/error_messages", resource: @profile %>

  <%= form_for(@profile, as: :profile, url: profile_path(@profile), method: :patch) do |f| %>
    <h1 class="h3 mb-3 fw-normal">Edit</h1>

    <div class="form-floating">
      <%= f.text_field :first_name, class: 'form-control', value: @profile.user.nil? ? '' : @profile.user.first_name, autofocus: true, autocomplete: "given-name", placeholder: 'First name' %>
      <%= f.label :first_name %>
    </div>

    <div class="form-floating">
      <%= f.text_field :last_name, class: 'form-control', value: @profile.user.nil? ? '' : @profile.user.last_name, autofocus: true, autocomplete: "family-name", placeholder: 'Family name'%>
      <%= f.label :last_name %>
    </div>

    <%= f.fields_for(:profile_attributes, include_id: false) do |profile_attributes_f| %>
      <div class="form-floating">
        <%= profile_attributes_f.email_field :email, class: 'form-control', autofocus: true, autocomplete: "email", placeholder: "example@mail.com"%>
        <%= profile_attributes_f.label :email %>
      </div>

      <div class="form-floating">
        <%= profile_attributes_f.phone_field :phone_number, class: 'form-control', autofocus: true, autocomplete: "phone", placeholder: "0999999999" %>
        <%= profile_attributes_f.label :phone_number %>
      </div>
    <% end %>

    <%= f.submit "Update", class: 'w-100 btn btn-lg btn-primary' %>
  <% end %>

  <%#= render "devise/shared/links" %>
</main>
